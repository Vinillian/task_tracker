name: android_studio_test
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.18.1
  cupertino_icons: ^1.0.2
  cloud_firestore: ^6.0.1
  firebase_auth: ^6.0.2
  firebase_core: ^4.1.0
  share_plus: ^12.0.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  file_picker: ^10.3.3
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  mockito: ^5.4.4
  build_runner: ^2.4.13
  hive_generator: ^2.0.1
  test: ^1.24.0


flutter:
  uses-material-design: true
