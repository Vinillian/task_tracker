name: task_tracker
description: "Task Tracker Application"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2  # ДОБАВИЛИ ЭТУ СТРОКУ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.5.1
  build_runner: ^2.8.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true